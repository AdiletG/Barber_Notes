spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

logging:
  level:
    root: warn
    org.springframework.security: warn
    kg.barbernotes.barbernotes: info